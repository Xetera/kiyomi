query Me {
  me {
    ...UserData
    images(orderBy: { createdAt: asc }) {
      ...GridImage
    }
  }
}

mutation toggleLike($id: Int!) {
  toggleLike(imageId: $id) {
    ...ImageData
    liked
  }
}
